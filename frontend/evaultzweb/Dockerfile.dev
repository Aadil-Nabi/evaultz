FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json ./
RUN npm install

# Copy source (will also be mounted by volumes in compose)
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
